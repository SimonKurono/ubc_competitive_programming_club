#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using vi = vector<int>;
using vll = vector<long long>;

void solve() {
    int n;
    cin >> n;

    // TODO: compute answer for this test case

    // Example output (replace):
    // cout << answer << "\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;                 // If problem has no t, delete this line and keep t=1
    while (t--) solve();

    return 0;
}
